from dwca.classes import DataFile


class GeologicalContext(DataFile):
    """
    Geological information, such as stratigraphy, that qualifies a region or place.
    """
    URI = DataFile.URI + "GeologicalContext"
